/**
 * ตัววาดแบบฟอร์ม — หัวใจของระยะที่ 2
 * อ่านนิยามช่องจาก List FormFields (L14) แล้วสร้างหน้าจอกรอก
 * มีตัวเดียวใช้ได้ทุกฟอร์ม เพิ่มฟอร์มใหม่ไม่ต้องแก้ไฟล์นี้
 *
 * ชนิดช่องที่ต้องรองรับ:
 * text textarea number currency date daterange time
 * choice multichoice lookup person yesno file table readonly
 */
export function renderForm(fields, values = {}) { /* ระยะที่ 2 */ }
export function collectValues(formEl, fields) { /* ระยะที่ 2 */ }
export function validate(fields, values) { /* ระยะที่ 2 */ }
